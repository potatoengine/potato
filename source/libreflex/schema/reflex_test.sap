module reflex_test;

enum TestEnum {
    First,
    Second,
    Third
}

struct TestStruct {
    TestEnum test = First;
}

struct TestComplex {
    float[] values;
    string name;
    TestStruct test;
}
