module config;

import common;

[serialize]
struct ImportMapping {
    [required]
    string pattern;
    [required]
    string importer;
}

[serialize]
struct Config {
    [required]
    string project;
    ImportMapping[] mapping;
    bool server = false;
}
